# Passation lot 8 — Accueil / bilans UI V4

Statut : validé. Branche : `feat/ui-v4`. Code : `030e5e1`. Session Claude Code, Sonnet 5 / medium (abonnement Pro).

Modifications (seuls fichiers du périmètre) : `Pages/Today.razor`, `Pages/Today.razor.css`, `Pages/History.razor`, `Pages/History.razor.css`, `Pages/Progress.razor`, `Pages/Progress.razor.css` réécrits en V4. Aucun changement partagé (composants Shared/, css/v4.css inchangés), endpoint, modèle ou migration.

- `Today.razor` (`/`) : `V4Header` (titre = salutation avec prénom réel via `AuthenticationState`, sous-titre = date du jour capitalisée ou « Une nouvelle journée commence. » si aucune séance) ; carte séance/cardio du jour avec CTA (« Commencer la séance » / « Voir la séance ») ou état vide avec deux `V4Button` (musculation/cardio) ; carte « Cette semaine » (séances/volume/temps, logique reprise à l'identique) ; **nouveau** : section « Tes séances » avec un graphique en barres par jour (lundi→dimanche, barre du jour courant en citron `#D4F53C`) affiché dès qu'il y a au moins une séance dans la semaine — remplace l'absence de calendrier du code pré-V4, répond à l'exigence du contrat (« Calendrier à partir des vraies séances ») ; si la semaine est vide, une carte « Préparer un carnet » (lien `/programs`) est affichée à la place ; carte « Reprendre » conservée (dernière séance différente d'aujourd'hui).
- `History.razor` (`/history`) : `V4Header`, segmented control local (Tous/Muscu/Cardio, `role="tablist"`), `V4Card` par entrée groupée par période (Cette semaine / Semaine dernière / Plus tôt), icônes SVG recolorées aux tokens V4 (encre `#082D45` / citron `#D4F53C`), `V4Loading`/`V4State` (erreur réseau avec `OnRetry`, aucune séance). Ajout d'une gestion d'erreur réseau explicite (absente du code pré-V4, qui ne distinguait pas chargement/erreur).
- `Progress.razor` (`/progress`) : `V4Header` (sous-titre « Semaine du {lundi} »), carte minutes de la semaine + delta, paire de `V4Card` (série en cours, séances cette semaine avec badge « Objectif atteint »), graphique en barres « Séances par jour », carte footer statique « Continue sur ta lancée ». `V4Loading`/`V4State` (erreur + retry, absente du code pré-V4). Objectif hebdomadaire : constante locale `WeeklyGoalDefault = 4` (commentée) en préparation du branchement `WeeklyGoalService` prévu lot 9 (Profil) — aucun service partagé créé dans ce lot, hors périmètre des fichiers autorisés.

Référence design — Pencil MCP (nœuds relus, pas tout le document) : `D6RYjE` (03 · Aujourd'hui), `XtbMO` (17 · Historique général), `iBD5O` (18 · Progrès), `VLQXJ` (24 · Accueil sans séance), `w8VzE` (26 · Chargement). Textes et structure comparés aux captures Playwright à 390 px : titres, libellés (« Ta séance du jour », « Cette semaine », « Tes séances », « Ton historique », « Tes progrès », « Séances par jour », « Objectif atteint », « Préparer un carnet », « Continue sur ta lancée ») et disposition correspondants. Écart documenté : l'état de chargement utilise `V4Loading` (squelettes génériques déjà validés lots 1-7) plutôt que les trois blocs illustrés spécifiquement dans `w8VzE`, cohérent avec tous les écrans V4 déjà livrés.

Validations : `dotnet build SportTracker.App/SportTracker.App.csproj --no-restore` → 0 avertissement, 0 erreur. Recette Playwright MCP isolée visible, API 5294 (SQLite temporaire du scratchpad, `--no-launch-profile`, `ASPNETCORE_ENVIRONMENT=Development` explicite pour la policy CORS locale, `ConnectionStrings__DefaultConnection` chemin Windows natif) + App 5281 (`--no-launch-profile`), compte synthétique `lot8-accueil@sporttracker.local`, séances créées via l'API (Haut du corps aujourd'hui, Run matinal cette semaine, Full body semaine dernière) :
- État vide (avant création des séances) : hero « Aucune séance aujourd'hui », deux CTA, stats à 0, carte « Préparer un carnet » — conforme à `VLQXJ`.
- État chargé : hero séance du jour, stats semaine (2 séances/400 kg/1h16), graphique en barres (barre du jour en citron), carte Reprendre « Full body » — conforme à `D6RYjE`.
- Historique : filtres Tous/Muscu/Cardio fonctionnels (vérifié clic Muscu → liste filtrée), groupement par période correct.
- Progrès : minutes/delta, série en cours, séances/semaine, graphique par jour, carte footer — conforme à `iBD5O`.
- Coupure API (arrêt du process pendant navigation sur Progrès) → `V4State` erreur + Réessayer affiché ; API relancée sur la même base temporaire → Réessayer recharge les données complètes sans perte.
- 320×720, 390×844, 1280×900 : aucun débordement horizontal (`scrollWidth === clientWidth` vérifié par script sur les 3 pages et les 3 tailles) ; layout centré sur desktop.
- Navigation clavier (Today) : lien d'évitement puis CTA « Commencer la séance » (ordre de focus correct, pas de piège).
- Console : uniquement logs HTTP normaux et l'erreur réseau volontairement provoquée pendant le test de coupure.

Écarts : le coordinateur complète le journal et la feature du vault puis réindexe QMD CLI collection `sporttracker` après la session. `CLAUDE.md` hors commit. `WeeklyGoalService` non créé (prévu lot 9, hors fichiers autorisés du contrat) ; valeur par défaut 4 codée en dur dans `Progress.razor` avec commentaire explicite.

Nettoyage : serveurs API/App isolés arrêtés (ports 5281/5294 libérés), base SQLite temporaire et captures Playwright supprimées du scratchpad et de la racine du dépôt.

Prochain lot : `09-profil/CONTRAT.md`, même checkout et branche, lancé par le coordinateur (aucun lot 9 lancé par cette session, conformément à l'instruction de cadrage).
