# Passation lot 3 — cardio UI V4

Statut : validé après reprise Sol medium. Branche : `feat/ui-v4`. Code initial : `e6981f8`, ajustements après recette : `9f44be87080cbe4f42087859e880eaa14a4d41a8`. Documentation précédente : `451dac2`, `9bec416`.

Modifications : les routes `/cardiosessions`, `/cardiosessions/new` et `/cardiosessions/{id}` utilisent désormais les composants V4, les cartes translucides, les actions citron et les marges de safe area. La liste couvre chargement, vide « Première sortie » et erreur réseau avec relance. Le formulaire garde ses valeurs après une erreur de sauvegarde, permet une nouvelle tentative et mène au détail créé. Le détail couvre chargement, 404 et erreur réseau avec relance.

Validations : build App final hors sandbox réussi, 0 avertissement et 0 erreur ; `git diff --check` OK. Recette réellement exécutée en UI authentifiée sur API locale et SQLite temporaire isolée, compte synthétique : liste vide, validation du nom manquant ; formulaire « Sortie reprise réseau », Vélo, 16/09/2026, 31 min, 5,2 km, 42 m ; arrêt API puis échec de sauvegarde, vérification DOM de tous les champs inchangés et activité Vélo toujours sélectionnée ; redémarrage API, clic Réessayer, ouverture du détail créé avec les bonnes valeurs. Liste et détail en erreur API puis relance réussie. 404 authentifié vérifié. Les frames Pencil WwhBX/AakVz/uyZUT/uTCoj ont été relues et comparées au rendu (fond, Foruner, cartes claires, CTA citron). Neuf contrôles responsive : trois pages à 320×720, 390×844 et 1280×900, largeur du document égale au viewport partout. À 320 px : date entièrement visible grâce à une colonne, sauvegarde/annulation accessibles par défilement et focus clavier.

Cause des tentatives précédentes résolue : l’app WASM doit être lancée via son DevServer (`dotnet run --no-build --no-launch-profile`) dans une session suivie, pas directement comme un exécutable backend ; API en Development avec CORS localhost explicite, redémarrage App après build pour éviter les empreintes WASM périmées. Aucun blocage critique restant, ni limitation de viewport dans cette reprise. Adaptations de maquette : activités en grille tactile 2×2, champs groupés dans V4Card, une colonne à 320 px ; pas de données fictives (zones, calories). Les trois CS7036 hérités restent reportés au lot 10. `CLAUDE.md` et la modification locale préexistante/automatique de `design.pen` restent hors commits ; aucune écriture Pencil métier n’a été demandée. Aucun endpoint, modèle, migration ou base existante/production touché.

Prochain lot : `04-carnets/CONTRAT.md` sur le même checkout et la même branche.
